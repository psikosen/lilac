[project]
name = "lilac"
version = "0.1.0"
description = "Small, reproducible PyTorch experiments for tiny reasoning models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "torch>=2.5",
  "pyyaml>=6.0",
  "safetensors>=0.4",
  "tokenizers>=0.20",
  "huggingface_hub>=0.25",
]

[tool.ruff]
line-length = 100
